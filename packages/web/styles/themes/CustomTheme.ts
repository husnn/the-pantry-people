import { createTheme } from '@mui/material/styles';

const CustomTheme = createTheme({
  palette: {
    secondary: {
      main: '#FFB33F',
      contrastText: '#FFFFFF'
    }
  }
});

export default CustomTheme;
